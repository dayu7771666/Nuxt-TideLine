# 开发环境语言配置
# 设置开发环境要加载的语言，可以显著提高启动速度

# 只加载英语（默认，最快启动）
# DEV_LANGUAGES=en

# 加载多个语言（用逗号分隔）
# DEV_LANGUAGES=en,fr,es

# 加载所有语言（与生产环境相同）
# DEV_LANGUAGES=all

# 可用的语言代码：
# ar (阿拉伯语), de (德语), en (英语), es (西班牙语), fr (法语)
# hi (印地语), it (意大利语), ja (日语), ko (韩语), pt (葡萄牙语)
# ru (俄语), zh (中文)

# 注意：生产环境 (NODE_ENV=production) 始终加载所有语言，忽略此设置
